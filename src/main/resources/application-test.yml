spring:
  datasource:
    url: jdbc:h2:mem:posreports;INIT=CREATE SCHEMA IF NOT EXISTS pos\;SET SCHEMA pos;
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console.enabled: true


